# 学习进度记忆功能 - 测试说明

## 功能说明
当您学习到某个单词后离开页面，再次进入时会自动从上次学习的位置继续，而不是从第一个单词重新开始。

## 测试步骤

### 1. 启动应用
```bash
npm run dev
```

### 2. 注册新账号
1. 访问 http://localhost:3000
2. 自动跳转到登录页
3. 点击"立即注册"
4. 填写邮箱、密码（至少6位）
5. 完成注册并自动登录

### 3. 开始学习
1. 如果是第一次使用，会进入引导页（Onboarding）
2. 完成引导后进入今日学习页面
3. 开始学习单词，通过翻译测验后查看单词详情

### 4. 测试进度保存
**步骤 A：学习几个单词**
1. 完成第1个单词的学习
2. 点击右箭头前进到第2个单词
3. 完成第2个单词的学习
4. 继续到第3、第4、第5个单词
5. 记住当前是第几个单词（例如：第5个）

**步骤 B：离开页面**
- 方式1：刷新浏览器（F5）
- 方式2：关闭标签页，重新打开
- 方式3：访问其他页面（如统计页），再回到主页

**步骤 C：验证位置恢复**
1. 重新进入学习页面
2. ✅ **应该显示第5个单词**（上次学习的位置）
3. ✅ **不应该从第1个单词开始**

### 5. 测试跨浏览器同步
**前提：使用同一个账号**
1. 在Chrome浏览器登录并学习到第10个单词
2. 打开Firefox浏览器
3. 使用相同账号登录
4. ✅ **应该从第10个单词继续**

### 6. 测试新的一天重置
**模拟方式（开发测试）：**
1. 清空浏览器localStorage中的`lastCheckDate`
2. 刷新页面
3. ✅ **应该重置到第1个单词**
4. ✅ **生成新的今日学习计划**

## 预期行为

### ✅ 正常情况
- 学习位置实时保存到数据库
- 刷新页面后位置保持不变
- 关闭浏览器后位置保持不变
- 在页面底部看到："3 / 50"（表示当前第3个单词，共50个）

### ✅ 新用户情况
- 首次登录从第1个单词开始
- 完成引导后从第1个单词开始
- 位置保存功能立即生效

### ✅ 新的一天
- 每天首次进入重置到第1个单词
- 开始新的学习计划
- 前一天的统计数据保留

## 调试信息

### 查看数据库（开发环境）
```bash
# 查看数据库文件
ls -lh data/app.db

# 使用sqlite3查看数据（如果已安装）
sqlite3 data/app.db "SELECT * FROM daily_progress;"
```

### 浏览器控制台
打开浏览器开发者工具（F12），查看 Console 标签：
- 应该看到 API 请求：`GET /api/progress`
- 应该看到位置更新：`POST /api/progress`
- 不应该有错误信息

### 检查网络请求
在 Network 标签中：
1. 筛选 XHR/Fetch 请求
2. 查找 `/api/progress` 请求
3. 检查响应数据中的 `current_word_index` 字段

## 常见问题

### Q1: 位置没有保存？
**检查项：**
- 是否已登录？（未登录无法保存）
- 是否通过了测验？（只有通过测验才会进入学习状态）
- 浏览器控制台有无错误？

### Q2: 每次都从第一个开始？
**可能原因：**
- 数据库未正确初始化
- API 请求失败
- 会话过期需要重新登录

### Q3: 数据库错误？
**解决方法：**
```bash
# 删除旧数据库重新初始化
rm data/app.db
npm run dev
# 重新注册账号
```

## 技术细节

### 数据保存时机
- 用户切换到下一个单词时
- 用户切换到上一个单词时
- 用户标记单词状态时
- 每次 currentWordIndex 变化时

### 数据库表结构
```sql
daily_progress:
- current_word_index: INTEGER -- 当前学习的单词索引
- user_id: TEXT -- 用户ID
- date: TEXT -- 日期 (YYYY-MM-DD)
```

### API 端点
- `GET /api/progress` - 获取今日进度
- `POST /api/progress` - 更新进度和位置

## 成功标志

✅ 学习到第N个单词
✅ 刷新页面
✅ 看到第N个单词（不是第1个）
✅ 页面顶部进度条显示正确位置

---

## 反馈
如果遇到问题，请提供：
1. 浏览器控制台的错误信息
2. 网络请求的响应内容
3. 具体的操作步骤
